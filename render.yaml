services:
  - type: web
    name: payment-parser
    runtime: node
    buildCommand: npm install
    startCommand: node bootstrap.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
